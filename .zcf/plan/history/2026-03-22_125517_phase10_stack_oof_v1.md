# Phase10 Stack OOF V1

- Status: `approved -> in_progress`
- Task: 基于现有 OOF 底座实现二层 stacking，争取形成超过当前最佳 blend 的新候选
- Constraint:
  - 本地仅做最小 smoke test
  - 完整 stacking 搜索与正式候选生成应在 Kaggle 远程执行
  - 当前仓库缺少部分 `phase2/3/6` 输出文件，远程正式执行前需补齐对应 OOF / submission 产物

## Objective

1. 实现 `phase10_stack_oof` 脚本与配置，支持基于现有 OOF / submission 进行二层 stacking 搜索。
2. 以当前最佳 blend 为参考基线：
   - OOF AUC: `0.9184677455`
   - Public LB: `0.91606`
3. 形成 1 个具备正式提交价值的 stacking 候选。

## Execution Plan

1. 新建 `kaggle_kernel/phase10_stack_oof`：
   - `stack_oof_search.py`
   - `stack_config_v1.json`
   - `stack_config_smoke.json`
   - `kernel-metadata.json`
2. 新建 `scripts/smoke/smoke_phase10_stack_oof.py`
3. `stack_oof_search.py` 支持：
   - 读取多模型 OOF / submission
   - 构建 `raw / rank / logit` meta 特征
   - 运行 `LogisticRegression / Ridge / XGBoost` meta learner
   - 与当前最佳 blend 做基线对比
   - 输出最佳候选 OOF / submission / 报告
4. 本地仅执行最小 smoke：
   - 从现有可用文件中抽样少量 train/test 行
   - 跑通脚本链路
   - 校验输出格式
5. 本轮执行完成后进入优化与评审阶段。

## Notes

1. 当前工作区中实际存在的可用输出主要是：
   - `phase8_catboost_strong/output_v2`
   - `phase9_realmlp_tabm_diverse/output_v2`
   - `phase7_blend_oof/output_phase9_realmlp_candidates`
2. `phase2/3/6` 的输出目录当前未随仓库同步，正式运行 `stack_config_v1.json` 前必须补齐。
