# Phase12 Rank Hybrid V1

- Status: `approved -> in_progress`
- Task: 基于 `phase11` 结果继续做窄区间 `rank hybrid` 搜索，争取超过当前 Public LB best `0.91608`。
- Constraint:
  - 本地仅做最小 smoke test
  - 正式候选生成、提交与计分仅在 Kaggle 远程执行
  - 继续复用 `chicachan/ps-s6e3-phase11-hybrid-inputs`，不新增模型池
  - 下载、上传与提交继续通过 `127.0.0.1:7890` 代理

## Objective

1. 复用 `phase11` 的二元 hybrid 脚本，但将候选空间收窄到 `rank` 融合。
2. 聚焦 `stack_weight` 在 `0.155 - 0.200` 区间的小步搜索。
3. 输出 1 个正式候选并提交，验证是否超过当前 best `0.91608`。

## Execution Plan

1. 优化 `phase11` 脚本中的 `rank` 计算缓存，减少重复排序。
2. 创建 `kaggle_kernel/phase12_rank_hybrid`
   - `hybrid_search.py`
   - `hybrid_config_v1.json`
   - `hybrid_config_smoke.json`
   - `kernel-metadata.json`
3. 新建 `scripts/smoke/smoke_phase12_rank_hybrid.py`
4. 本地仅执行：
   - `py_compile`
   - 抽样 smoke
5. Kaggle 远程执行、回收结果并正式提交。

## Notes

1. `phase11` 最佳 OOF 候选：
   - `rank_stack0.170`
   - OOF AUC: `0.9184687217`
   - Public LB: `0.91607`
2. 这说明 `rank hybrid` 路线成立，但当前需要更窄的权重微调，而不是继续扩候选空间。
